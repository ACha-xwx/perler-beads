# 发现与决定

## 需求确认
- 目标基线不是此前误改的 Beadloom/landing 方向，而是 `origins/index.html` 保存下来的工作台页面。
- 核心仍然是图片转拼豆，不需要改成宣传页。
- 右侧留言板、素材隐私声明、社区与支持等公开运营模块要移除，保留自用工具。
- 新名字需要英文，不参考“七卡瓦”，本次定为 `BeadForge`。
- `MARD 291` 的问题是对齐，不是样式需要重做，所以只把内部两行文字居中。
- 主题、字体、缩放参考 `D:\Personal Page\qbot-push-center\static` 的设置思路。

## 结构发现
- 原页面适合保留单页工作台结构：顶栏、画布、右侧设置面板、底栏。
- 顶栏承载品牌、模式切换、撤销、色板计数、导入、下载、保存和设置。
- 中央画布继续作为主工作区，导入前显示拖放入口，导入后显示拼豆画布。
- 右侧从公开内容改成外观、图纸、处理参数和颜色统计。

## 设计决定
| 决定 | 原因 |
|------|------|
| 默认浅色黑白主题 | 符合用户最新要求，且更接近工具型工作台 |
| 多个浅色主题 | 保留 qbot 那种可切换感，但不做重度深色界面 |
| 字体与缩放保存到本地 | 适合自用，避免每次打开都重新设置 |
| 按钮使用毛玻璃质感 | 提升科技感和设计感，但不改变工作流 |
| 动效使用 transform/opacity | 性能更稳，也符合 reduced-motion 要求 |

## 验证结论
- `npm run build` 通过。
- 本机 Chrome 可访问 `http://localhost:3000/` 并生成截图。
- Codex 内置浏览器连接 `localhost` 失败，但 PowerShell 请求返回 200，本机 Chrome 截图也成功，判断为内置浏览器 loopback 隔离问题。
